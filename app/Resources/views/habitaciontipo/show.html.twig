{% extends 'base.html.twig' %}

{% block body %}
    <h1>Habitaciontipo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ habitacionTipo.id }}</td>
            </tr>
            <tr>
                <th>Habitaciontipo</th>
                <td>{{ habitacionTipo.habitacionTipo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('habitaciontipo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('habitaciontipo_edit', { 'id': habitacionTipo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
