{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reserva creation</h1>
    
    <div class="row">
        <div class="col-md-6">
            <p>Buscar Huesped</p>
            <form class="form-inline" onsubmit="buscar(); return false;">
              <div class="form-group">
                <label class="sr-only" for="cliente">Email address</label>
                <input type="text" class="form-control" id="cliente" placeholder="Nro de documento">
              </div>
              <button type="submit" class="btn btn-default">Buscar</button>
            </form>
        </div>
        <div class="col-md-6" id="divReserva">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" />
            {{ form_end(form) }}
        </div>
    </div>

    <ul>
        <li>
            <a href="{{ path('reserva_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

{% block javascripts %}
    {{parent()}}
    <script type="text/javascript">
        var buscar = function(){
            $.ajax({
                method: 'POST',
                url: '{{path("reserva_buscarHuesped")}}',
                data: {id:2},
                beforeSend: function (){
                    
                },
                success: function(data){

                },
                error: function(){

                }
            });
        }
    </script>
{% endblock %}
