{% extends 'base.html.twig' %}

{% block body %}
    <header class="content__title">
        <h1>Crear nuevo  tipo de residencia</h1>
        <div class="actions">

        </div>
    </header>
    <div class="card">
        <div class="card-block">
            {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-6">
                        {{form_label(form.residencia)}}
                        {{form_widget(form.residencia, {'attr':{'class':'form-control'} })}}
                        <i class="form-group__bar"></i>
                    </div>
                    <div class="col-md-6">
                        {{form_label(form.abrev)}}
                        {{form_widget(form.abrev, {'attr':{'class':'form-control'} })}}
                        <i class="form-group__bar"></i>
                    </div>
                </div>
                {{ form_widget(form) }}
                <br>
                <button type="submit" class="btn btn-success">Registrar</button>
                <a href="{{ path('residenciatipo_index') }}" class="btn btn-warning">Cancelar</a>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
